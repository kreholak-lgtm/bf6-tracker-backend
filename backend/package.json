{
  "name": "backend",
  "version": "1.0.0",
  "description": "Serwer backend dla aplikacji Battlefield Tracker",
  "main": "dist/index.js",
  "scripts": {
    "dev": "nodemon --exec node index.js", // Zmiana na node
    "build": "echo 'No build step required for pure JS' && exit 0", // Uproszczono build
    "start": "node index.js", // Start czystego JS
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.7.2",
    "bcryptjs": "^2.4.3", // Używamy bezpiecznego bcryptjs
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "nodemailer": "^6.9.14",
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
```

### 🚀 Krok 3: Wypchnięcie zmian

1.  **Zmień nazwy plików:** Zmień nazwę pliku `backend/index.ts` na **`backend/index.js`**.
2.  **Zapisz plik `backend/package.json`** (z nową konfiguracją).
3.  **Zrób commit i wypchnij zmiany:**
    ```bash
    git add .
    git commit -m "Final FIX: Switched to pure JS and simplified npm scripts to bypass native module errors."
    git push origin main
    
