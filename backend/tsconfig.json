{
  "compilerOptions": {
    "target": "ES2020",
    "module": "CommonJS",
    "rootDir": "./",
    "outDir": "./dist",
    "esModuleInterop": true,
    "forceConsistentCasingInFileNames": true,
    
    "strict": false,               
    "noImplicitAny": false,        
    "strictPropertyInitialization": false, 
    
    "skipLibCheck": true,
    "resolveJsonModule": true,

    "allowJs": true,
    "checkJs": false, 
    "moduleResolution": "Node",
    // --- POPRAWKA TUTAJ: USUNIĘTO STARE @types/bcrypt, DODANO JAWNY bcryptjs ---
    "types": ["node", "axios", "jsonwebtoken", "nodemailer", "pg", "cors", "bcryptjs"] 
    // --------------------------------------------------------------------------
  },
  "include": [
    "./**/*.ts",
    "./utils/**/*.js" 
  ],
  "exclude": [
    "node_modules",
    "**/*.spec.ts",
    "**/*.test.ts"
  ]
}
```

### 🚀 Następne kroki:

1.  **Zapisz plik `backend/tsconfig.json`** w Canvas.
2.  **Zrób commit i wypchnij zmiany** do GitHub:
    ```bash
    git add .
    git commit -m "Final critical fix: Synced tsconfig.json types for bcryptjs on Render"
    git push origin main
    
