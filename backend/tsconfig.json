{
  "compilerOptions": {
    "target": "ES2020",
    "module": "CommonJS",
    "rootDir": "./",
    "outDir": "./dist",
    "esModuleInterop": true,
    "forceConsistentCasingInFileNames": true,
    "strict": true,
    "skipLibCheck": true,
    "resolveJsonModule": true,

    "allowJs": true,
    "checkJs": true,
    "moduleResolution": "Node",
    "types": ["node", "axios"]
  },
  "include": [
    "./**/*.ts",
    "./**/*.js" 
  ],
  "exclude": [
    "node_modules"
  ]
}
```

### 🚀 Następne kroki:

1.  **Zastąp** zawartość pliku `backend/tsconfig.json` w Canvas powyższym kodem.
2.  **Zrób commit i wypchnij zmiany** do GitHub:
    ```bash
    git add .
    git commit -m "Fix: Added 'axios' to types in tsconfig to resolve .get() error on Render"
    git push origin main
    
